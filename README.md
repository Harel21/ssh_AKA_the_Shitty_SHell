# ssh_AKA_the_Shitty_SHell
ssh is "the Shitty SHell" maade in C

#BUILDING
To build the project create a directory named "build" where you downloaded the code and run `cmake -S . -B build/` in the directory of the source code 
